#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);//fast input

    int a,b;
    cin>>a>>b;
    if(a>b)
    cout<<"greater\n";
    else
    cout<<"lower\n";
    return 0;//ios_base::sync_with_stdio(false);cin.tie(NULL)
}