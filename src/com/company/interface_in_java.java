package com.company;
/*java interface is a group of related methods with empty bodies
* object of interface cannot be created but references can be created
*
* in abstract class both abstract and non abstract methods can be present but in interface only abstarct methods are present */
public class interface_in_java {
    public static void main(String args[]){
        System.out.println("Interface");
    }
}
